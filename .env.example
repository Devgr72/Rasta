# Copy to .env — this file is read only by vision.js and never reaches the browser.
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Optional overrides
# RASTA_MODEL=claude-fable-5-1
# RASTA_MOCK_VISION=1   # exercise the pipeline without spending tokens
# PORT=3000
